CREATE OR REPLACE PROCEDURE P_DM_BRANCH
(IO_STATUS OUT INTEGER,IO_SQLERR OUT VARCHAR2)
AS

	TABLE_NAME VARCHAR2(125) :='BRANCH';  -- 表名(修改)
BEGIN
/*
--编辑取数SQL
DELETE MMAPDM.MMAPDM_PROC_SQL WHERE TABLE_NAME='DM_BRANCH';
COMMIT;
INSERT INTO MMAPDM.MMAPDM_PROC_SQL(TABLE_NAME,DMSQL,PROC_NAME) VALUES('DM_BRANCH','
      SELECT
        TO_NUMBER(TO_CHAR((SYSDATE),''YYYYMMDD''))
        ,TX_DATE
        ,PERIOD_ID
        ,REGION_ID
        ,REGION_NM
        ,FBRNH_ID
        ,FBRNH_NM
        ,BRNH_ID
        ,BRNH_NM
        ,EQUIPMENT_ID
        ,CHANNEL
        ,TELEPHONE
        ,ADDR
        ,SOURCEGRP
      FROM MMAPST.ST_BRANCH','P_DM_BRANCH')
;
COMMIT;
*/
   -- P_DM_FULL(TABLE_NAME,IO_STATUS,IO_SQLERR);    
    P_DM_FULL(TABLE_NAME,10000,IO_STATUS,IO_SQLERR);
END P_DM_BRANCH;
/
