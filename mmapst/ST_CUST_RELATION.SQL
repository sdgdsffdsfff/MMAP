DROP TABLE ST_CUST_RELATION;
CREATE TABLE MMAPST.ST_CUST_RELATION
(
	 ETL_DATE NUMBER(8)
	,TX_DATE DATE
	,PERIOD_ID NUMBER(8)
	,CIF_ORG VARCHAR2(50)
	,RELCIF_ORG VARCHAR2(50)
	,RELTYP_CODE VARCHAR2(50)
	,RELTYP VARCHAR2(50)
);
INSERT INTO MMAPST.ST_CUST_RELATION 
(
	 ETL_DATE
	,TX_DATE
	,PERIOD_ID
	,CIF_ORG
	,RELCIF_ORG
	,RELTYP_CODE
	,RELTYP
) 
SELECT
	 TO_NUMBER(TO_CHAR(SYSDATE,'YYYYMMDD')) AS ETL_DATE  
	,TX_DT
	,TO_NUMBER(TO_CHAR(SYSDATE,'YYYYMMDD')) AS PERIOD_ID
	,TRIM(CIF_ORG)
	,TRIM(NVL(CIFTYP_ID,'-'))
	,TRIM(NVL(RELCIF_ORG,'-'))
	,TRIM(NVL(RELTYP_ID,'-'))
FROM MMAPST.DMMKT_CSCIFREL